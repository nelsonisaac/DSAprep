class HouseRobber {
    public int rob(int[] nums) {
        // TODO: Implement O(N) time and O(1) space choice-based DP.
        // 1. Handle edge cases when nums.length is 0 or 1.
        // 2. Maintain two state variables: robPrev2 and robPrev1.
        // 3. Loop through nums, updating states: nextRob = max(nums[i] + robPrev2, robPrev1).
        // 4. Return the maximum value robbed.
        return 0;
    }
}

