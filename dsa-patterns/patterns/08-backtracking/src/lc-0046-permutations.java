import java.util.ArrayList;
import java.util.List;

class Permutations {
    public List<List<Integer>> permute(int[] nums) {
        // TODO: Implement recursive backtracking to find all permutations.
        // 1. Initialize a result list of lists.
        // 2. Call helper backtrack function.
        // 3. Base case: if current list size matches nums length, add copy to result.
        // 4. Loop through nums. If already chosen, skip. Else, add, recurse, backtrack.
        return new ArrayList<>();
    }
}

