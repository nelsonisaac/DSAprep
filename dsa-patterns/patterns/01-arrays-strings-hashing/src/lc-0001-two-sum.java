import java.util.HashMap;
import java.util.Map;

class TwoSum {
    public int[] twoSum(int[] nums, int target) {
        // TODO: Implement the O(N) HashMap-based solution here.
        // 1. Initialize a Map to store values and their indices.
        // 2. Iterate through nums, calculate the complement.
        // 3. Check if complement exists, return indices if found.
        // 4. Otherwise, put current number and index in the Map.
        return new int[0];
    }
}

