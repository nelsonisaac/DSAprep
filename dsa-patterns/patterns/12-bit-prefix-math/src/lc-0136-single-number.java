class SingleNumber {
    public int singleNumber(int[] nums) {
        // TODO: Implement the O(N) time and O(1) space XOR trick.
        // 1. Initialize result = 0.
        // 2. Loop through nums and apply bitwise XOR operator.
        // 3. Return the result.
        return 0;
    }
}

