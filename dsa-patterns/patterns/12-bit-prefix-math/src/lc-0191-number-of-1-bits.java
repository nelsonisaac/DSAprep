class NumberOf1Bits {
    // You may treat n as an unsigned value
    public int hammingWeight(int n) {
        // TODO: Implement set bit count (e.g. Brian Kernighan's n & (n - 1) bit-clearing loop).
        // 1. Initialize count = 0.
        // 2. Loop while n != 0.
        // 3. Clear lowest set bit: n = n & (n - 1), increment count.
        // 4. Return count.
        return 0;
    }
}

