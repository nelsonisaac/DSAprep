import java.util.Stack;

class EvaluateRPN {
    public int evalRPN(String[] tokens) {
        // TODO: Implement RPN evaluation using a stack.
        // 1. Initialize a Stack of Integers.
        // 2. Loop through each token.
        // 3. If operator: pop two values, perform operation (a operator b), push result.
        // 4. If operand: parse to int, push to stack.
        // 5. Return the final remaining stack value.
        return 0;
    }
}

