import java.util.PriorityQueue;

class KthLargestElement {
    public int findKthLargest(int[] nums, int k) {
        // TODO: Implement O(N log K) min-heap solution.
        // 1. Initialize a Min-Heap: PriorityQueue<Integer>.
        // 2. Loop through nums. Offer each element to the heap.
        // 3. If heap size exceeds k, poll the smallest out.
        // 4. Return heap.peek().
        return 0;
    }
}

