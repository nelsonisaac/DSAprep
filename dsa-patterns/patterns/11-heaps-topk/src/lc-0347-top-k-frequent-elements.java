import java.util.HashMap;
import java.util.Map;
import java.util.PriorityQueue;

class TopKFrequent {
    public int[] topKFrequent(int[] nums, int k) {
        // TODO: Implement O(N log K) map + priority queue frequency search.
        // 1. Count element frequencies using a Map.
        // 2. Maintain a size-k Min-Heap ordered by frequency: PriorityQueue<Integer> custom comparison.
        // 3. Extract heap contents into integer array and return.
        return new int[0];
    }
}

