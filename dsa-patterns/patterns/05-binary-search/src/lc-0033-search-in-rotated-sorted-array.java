class SearchRotatedArray {
    public int search(int[] nums, int target) {
        // TODO: Implement O(log N) search in a rotated sorted array.
        // 1. Maintain left and right pointers.
        // 2. Locate which half is sorted (left or right).
        // 3. Check if target lies within the boundaries of the sorted half.
        // 4. Update left and right pointers accordingly.
        return -1;
    }
}

