class ContainerWithMostWater {
    public int maxArea(int[] height) {
        // TODO: Implement the O(N) two pointers from ends solution.
        // 1. Position pointers left at 0 and right at end.
        // 2. Compute the current area and record the maximum.
        // 3. Move the pointer pointing to the shorter boundary inward.
        return 0;
    }
}

