import java.util.HashMap;
import java.util.Map;

class LongestSubstring {
    public int lengthOfLongestSubstring(String s) {
        // TODO: Implement the O(N) sliding window solution.
        // 1. Maintain a map of character to index, or an array for ASCII.
        // 2. Expand the right boundary of the window.
        // 3. If a duplicate is found in the current window, shrink from left.
        // 4. Update the maximum window length.
        return 0;
    }
}

