import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

class ThreeSum {
    public List<List<String>> threeSum(int[] nums) {
        // Note: Signature matching LeetCode uses List<List<Integer>>. 
        // Using List<List<Integer>> for consistency with practice.
        return new ArrayList<>();
    }

    public List<List<Integer>> solveThreeSum(int[] nums) {
        // TODO: Implement the O(N^2) sort and two-pointer solution.
        // 1. Sort the input array.
        // 2. Loop through nums up to length-2 (index i).
        // 3. Skip duplicates for i.
        // 4. Initialize left = i + 1, right = length - 1.
        // 5. Use two-pointers to find pairs summing to -nums[i].
        return new ArrayList<>();
    }
}

